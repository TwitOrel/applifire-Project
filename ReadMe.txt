run the REST API:
    ./manage.py runserver

run the ngrok:
    ngrok http 8000

define:
    # DataBase
        DB_NAME
        DB_USER
        DB_PASSWORD
        DB_HOST = localhost // HOST
        DB_PORT

    # Email for sending mails
        EMAIL_HOST_USER = email who send the mails
        EMAIL_HOST_PASSWORD = password for the appliction

    # OAuth
        GOOGLE_CLIENT_ID 
        GOOGLE_CLIENT_SECRET
